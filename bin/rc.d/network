#!/bin/sh

. /etc/rc.conf

case "$1" in
	-s)
		ifconfig $INTERFACE up && sdhcp $INTERFACE
		;;
	-k)
		pkill sdhcp
		ifconfig $INTERFACE down
		;;
	-r)
		pkill sdhcp
		ifconfig $INTERFACE down
		ifconfig $INTERFACE up && sdhcp $INTERFACE
		;;
	*)
		echo "usage: $0 [-s|-k|-r]"
esac
exit 0
